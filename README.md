# Hackerfab-Stepper

in default.toml: change the port to what new label shows up when u connect the board with ur computer and run ls /dev/tty.* (it was /dev/tty.usbmodem3101 for me) (the exact terminal commands might be different based on MacOS but that’s the idea)

running gui.py: connect computer to board & USB cam in setup
